Install ->
	streamlit

Verify ->
	streamlit --version

Run Streamlit App ->
	streamlit run streamlit_app.py

	if above command not worked, use the following command

	python -m streamlit run streamlit_app.py